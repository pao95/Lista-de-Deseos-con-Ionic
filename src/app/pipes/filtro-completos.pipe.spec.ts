import { FiltroCompletosPipe } from './filtro-completos.pipe';

describe('FiltroCompletosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCompletosPipe();
    expect(pipe).toBeTruthy();
  });
});
